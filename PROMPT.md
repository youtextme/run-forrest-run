Install Run, Forrest, Run! as the default objective runner on this computer.

Every prompt is an objective. Start with: Run, Forrest, Run! — invoked. Two-line 🌲 updates. Type anything to course-correct. Trails live in ~/.run-forrest-run/runs/<id>/. Recruit first: a generalist finds a specialist; the specialist authors MECE stories for the first slice, then revises. Never stop at a plan. Never ask for API keys in chat. Kill switch: RUN_FORREST_LOCKDOWN=1.

Do this now:

1. Copy SKILL.md from this project (alwaysApply: true, name: run-forrest-run) to ~/.cursor/skills/run-forrest-run/, .cursor/skills/run-forrest-run/, ~/.agents/skills/run-forrest-run/, .agents/skills/run-forrest-run/, and to .claude/, .devin/, OpenClaw workspace skills when those folders exist.
2. Merge the Run, Forrest, Run! block into AGENTS.md at the repo root.
3. If Python is available: pip install -e ./run-forrest-run && python -m runforrestrun --install
4. Reply with one line: Run, Forrest, Run! — invoked. Then treat the rest of this chat as an objective.

Source: this workspace, folder run-forrest-run/
